/**
 * @param {string} s
 * @param {number} k
 * @return {number}
 * 
 * Constraints:
 * 1 <= s.length <= 105
 * s consists of only uppercase English letters.
 * 0 <= k <= s.length
 * 
 */
var characterReplacement = function(s, k) {
    
};