/**
 * @param {string[]} strs
 * @return {string[][]}
 * 
 * Constraints:
 * 1 <= strs.length <= 104
 * 0 <= strs[i].length <= 100
 * strs[i] consists of lowercase English letters.
 */

var groupAnagrams = function(strs) {
    
};